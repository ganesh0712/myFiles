import React from 'react'

function Homepg() {
    return (
        <div>
            <p style={{backgroundColor : mode}}>Linting is the process of running a program that will analyse code for potential errors. 
                See lint on wikipedia: lint was the name originally given to a particular 
                program that flagged some suspicious and non-portable constructs (likely to be bugs) 
                in C language source code.</p>
                <button onClick= >Dark</button>
                <button >Day</button>
        </div>
    )
}

export default Homepg
